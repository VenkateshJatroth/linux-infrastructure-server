server {
    listen 443 ssl;

    server_name venkytech.com;

    root /var/www/venkytech;
    index index.html;

    ssl_certificate /etc/ssl/certs/venkytech.crt;
    ssl_certificate_key /etc/ssl/private/venkytech.key;

    location / {
        try_files $uri $uri/ =404;
    }
}